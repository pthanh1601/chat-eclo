export function generateJitsiJWT(openIdToken: any, confId: string, domain: string, displayName: string, avatarUrl: string): string {
  // Mock JWT for Jitsi. In real implementation, this should be signed.
  return "";
}
